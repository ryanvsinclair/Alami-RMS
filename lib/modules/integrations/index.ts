import type { ModuleDefinition } from "@/core/types/module";

export const integrationsModule: ModuleDefinition = {
  id: "integrations",
  name: "Integrations",
  description: "External revenue and POS sync integrations",
  navItems: [],
};