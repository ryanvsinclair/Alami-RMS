/**
 * Transitional wrapper during app-structure refactor.
 * Canonical shared flow component entry point (currently re-exporting legacy implementation).
 */

export * from "../../../../components/flows/item-not-found";
