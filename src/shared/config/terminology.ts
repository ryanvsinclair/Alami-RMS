/**
 * Transitional wrapper during app-structure refactor.
 * Canonical shared config entry point (currently re-exporting legacy implementation).
 */

export * from "../../../lib/config/terminology";
