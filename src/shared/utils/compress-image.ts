/**
 * Transitional wrapper during app-structure refactor.
 * Canonical shared client utility entry point (currently re-exporting legacy implementation).
 */

export * from "../../../lib/core/utils/compress-image";
