/**
 * Transitional wrapper during app-structure refactor.
 * Canonical matching engine entry point (currently re-exporting legacy implementation).
 */

export * from "../../../lib/core/matching/engine";
