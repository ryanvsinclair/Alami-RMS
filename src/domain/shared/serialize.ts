/**
 * Transitional wrapper during app-structure refactor.
 * Canonical domain/shared serialize entry point (currently re-exporting legacy implementation).
 */

export * from "../../../lib/core/utils/serialize";
