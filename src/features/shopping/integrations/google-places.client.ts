/**
 * Transitional wrapper during app-structure refactor.
 * Canonical shopping Google Places client entry point (currently re-exporting legacy implementation).
 */

export * from "../../../../lib/google/places";
