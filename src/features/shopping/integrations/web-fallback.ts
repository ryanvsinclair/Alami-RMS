/**
 * Transitional wrapper during app-structure refactor.
 * Canonical shopping web fallback integration entry point (currently re-exporting legacy implementation).
 */

export * from "../../../../lib/modules/shopping/web-fallback";
