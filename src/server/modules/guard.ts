/**
 * Transitional wrapper during app-structure refactor.
 * Canonical server module guard entry point (currently re-exporting legacy implementation).
 */

export * from "../../../lib/core/modules/guard";
