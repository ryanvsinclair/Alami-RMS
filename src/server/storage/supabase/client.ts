/**
 * Transitional wrapper during app-structure refactor.
 * Canonical Supabase server client entry point (currently re-exporting legacy implementation).
 */

export * from "../../../../lib/supabase/server";
