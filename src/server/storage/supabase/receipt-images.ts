/**
 * Transitional wrapper during app-structure refactor.
 * Canonical Supabase receipt image storage entry point (currently re-exporting legacy implementation).
 */

export * from "../../../../lib/supabase/storage";
