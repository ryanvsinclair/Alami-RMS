/**
 * Transitional wrapper during app-structure refactor.
 * Canonical server matching adapter entry point (currently re-exporting legacy implementation).
 */

export * from "../../../lib/core/matching/receipt-line";
