/**
 * Transitional wrapper during app-structure refactor.
 * Canonical server/auth tenant entry point (currently re-exporting legacy implementation).
 */

export * from "../../../lib/core/auth/tenant";
