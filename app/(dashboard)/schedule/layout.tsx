/**
 * Schedule route layout.
 * No module gate — Schedule is a core feature available to all businesses.
 */
export default function ScheduleLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
